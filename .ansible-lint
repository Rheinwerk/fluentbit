---
exclude_paths:
  - .cache/
  - .github/
  - .gitlab-ci.yml

parseable: true

offline: true

warn_list:
  - name[template]
  - role-name
  - yaml[line-length]
  - key-order[task] # additional
  - no-changed-when
